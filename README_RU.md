## [English version](README.md)

Aggregator Service (Сервис-Агрегатор)

Этот проект представляет собой REST API сервис, который служит единой точкой доступа к данным из различных внешних источников. Его основная задача — асинхронно собирать, унифицировать, кэшировать и отдавать информацию (новости и погоду) по запросу клиента.
Проект создан для демонстрации навыков  в бэкенд-разработке, с фокусом на создании надежных, поддерживаемых и производительных систем.

🚀 Ключевые возможности

Агрегация: Сбор данных из нескольких сторонних API (NewsAPI.org и OpenWeatherMap).
Автоматизация: Встроенный планировщик (Cron) автоматически запускает сбор данных по расписанию.
Производительность: Кэширование "горячих" данных (новости) в Redis для быстрой отдачи.
REST API: Предоставление собранных данных через JSON API (/api/v1/news, /api/v1/weather).
Чистая архитектура: Рразделение ответственности (Провайдеры данных, Контроллеры, Модели).
Окружение: Полностью настроенное локальное окружение на Docker с использованием Laravel Sail.

🛠️ Стек и технологии

Backend: PHP 8.3+ / Laravel 11
Базы данных: MySQL (для хранения данных)
Кэш: Redis (для кэширования API-ответов)
Инфраструктура: Docker (Laravel Sail)
Автоматизация: Cron + Laravel Scheduler

🏁 Установка и запуск (локально)

1. Клонируйте репозиторий:
git clone [https://github.com/saken555/AggregatorService.git](https://github.com/saken555/AggregatorService.git)
cd AggregatorService

2. Скопируйте файл окружения:
cp .env.example .env

3. Установите API-ключи:
Откройте файл .env и добавьте ваши ключи от сервисов:

NEWS_API_KEY=ВАШ_КЛЮЧ_ОТ_NEWSAPI
OPENWEATHERMAP_API_KEY=ВАШ_КЛЮЧ_ОТ_OPENWEATHERMAP

4. Запустите Docker-контейнеры:
./vendor/bin/sail up -d

5. Установите зависимости:
./vendor/bin/sail composer install

6. Сгенерируйте ключ приложения:
./vendor/bin/sail artisan key:generate

7. Запустите миграции базы данных:
./vendor/bin/sail artisan migrate

8. Настройте Cron (Автоматизация):
Чтобы сбор данных работал автоматически, добавьте эту строку в ваш crontab (выполните crontab -e):
* * * * * cd /путь/к/вашему/проекту/AggregatorService && ./vendor/bin/sail artisan schedule:run >> /dev/null 2>&1

(Замените /путь/к/вашему/проекту на ваш реальный путь)

🚀 Использование

Сервис будет доступен по адресу http://localhost.
Ручной сбор данных (для теста)
Вы можете запустить сбор данных вручную в любой момент:

# Собрать новости
./vendor/bin/sail artisan app:fetch-news

# Собрать погоду
./vendor/bin/sail artisan app:fetch-weather


API Эндпоинты
Сервис предоставляет следующие JSON API эндпоинты:

1. Получить новости
URL: GET /api/v1/news
Описание: Возвращает 25 последних новостей. Ответ кэшируется в Redis на 10 минут.
Пример запроса:
curl http://localhost/api/v1/news


2. Получить погоду
URL: GET /api/v1/weather
Описание: Возвращает самое последнее сохраненное наблюдение о погоде.
Пример запроса:
curl http://localhost/api/v1/weather
